# Core MVP Blocks 1-3 Cleanup Acceptance Audit

Date: 2026-05-27

## Scope

This audit closes `docs/exec_plans/core_mvp_blocks_1_3_cleanup_plan.md` Session 6.
It verifies the current portfolio-first live path after Sessions 1-5. Generated artifacts were
regenerated by the live command and inspected as evidence only; no generated artifact was hand-edited
or treated as source truth.

## Commands Run

```powershell
.\.venv\Scripts\python.exe run_portfolio_review.py --skip-candidates
@'PY' | .\.venv\Scripts\python.exe -
# JSON acceptance probe over Main portfolio/analysis_subject outputs
PY
.\.venv\Scripts\python.exe -m pytest tests/test_core_mvp_blocks_1_3_boundaries.py tests/test_input_assumptions.py tests/test_real_cash.py tests/test_portfolio_xray_contract.py tests/test_stress_diagnostic_mode.py tests/test_stress_results_block_contract.py tests/test_hedge_gap_analysis_v1_contract.py tests/test_current_portfolio_stress_scorecard_v1_contract.py
.\.venv\Scripts\python.exe scripts\verify_docs.py
.\.venv\Scripts\python.exe -m pytest
```

## Live Output Evidence

Live portfolio-first run completed successfully and regenerated:

- `Main portfolio/analysis_subject/run_metadata.json`
- `Main portfolio/analysis_subject/portfolio_xray.json`
- `Main portfolio/analysis_subject/stress_report.json`
- `Main portfolio/scenario_library.json`

Acceptance probe result: `LIVE_ACCEPTANCE_OK`.

Observed evidence:

- Core MVP input surface: `analysis_setup.core_mvp_input_surface.version = core_mvp_input_surface_v1`.
- Core MVP required user input groups: `tickers`, `allocation`, `investor_currency`.
- Block 2 product keys present:
  - `block_2_1_asset_allocation`
  - `block_2_2_portfolio_metrics`
  - `block_2_3_factor_exposure`
  - `block_2_4_hidden_exposure`
  - `block_2_5_risk_budget_view`
  - `block_2_6_portfolio_weakness_map`
- `portfolio_xray.json.legacy_summary._scope.product_surface = false`.
- `legacy_summary.portfolio_diagnostic_verdict` does not expose `mandate_gate`.
- Block 3 product keys present in `stress_report.json`:
  - `stress_results_v1`
  - `hedge_gap_analysis_v1`
  - `current_portfolio_stress_scorecard_v1`
- `stress_report.loss_gate_mode = diagnostic`.
- Raw `scenario_results` rows: 8 rows; no row-level `pass`, `loss_ok`, `diagnostic_code`, or `diagnostic_codes` keys.
- Raw `historical_results` rows: 5 rows; no row-level `pass`, `loss_ok`, `diagnostic_code`, or `diagnostic_codes` keys.
- `stress_results_v1.synthetic[]` / `historical[]` do not expose row-level mandate/pass-fail fields.
- Scenario Library pattern matches current architecture: `stress_report.scenario_library_meta` plus sidecar `Main portfolio/scenario_library.json`; no top-level `stress_report.scenario_library_v1` key.

## Test Results

Targeted Core MVP Blocks 1-3 acceptance suite:

```text
156 passed in 7.15s
```

Docs verification:

```text
docs verification: OK
```

Full repository pytest was also run as requested by the plan. It completed collection/execution but is
not globally green in the current repository state:

```text
9 failed, 1171 passed, 2 skipped, 1 warning in 660.83s
```

Failing tests observed:

- `tests/test_candidate_comparison.py::test_current_unavailable_in_optimize_mode`
- `tests/test_candidate_factory_contract.py::test_live_factory_build_matches_golden_document`
- `tests/test_current_vs_policy_workflow.py::test_combined_context_both_available`
- `tests/test_current_vs_policy_workflow.py::test_current_weights_without_sidecar`
- `tests/test_factor_covariance.py::test_factor_covariance_empty_factor_frame_returns_explicit_skip_reason`
- `tests/test_live_core_e2e_validation.py::test_validate_live_core_artifacts_accepts_seeded_core_workspace`
- `tests/test_live_full_e2e_validation.py::test_validate_live_full_artifacts_accepts_seeded_full_workspace`
- `tests/test_live_full_e2e_validation.py::test_validate_live_full_resume_expects_manifest`
- `tests/test_mvp_workflow.py::test_policy_current_adds_materialize_when_weights_set`

These failures are outside the Core MVP Blocks 1-3 cleanup acceptance surface verified by the targeted
suite above. They should be handled as separate follow-up work if global pytest green is required.

## Acceptance Decision

Core MVP Blocks 1-3 cleanup is accepted for the portfolio-first diagnostics path:

- Block 1 minimal input product contract is clean.
- Real cash regression coverage remains green.
- Block 2 product keys 2.1-2.6 are present and remain diagnostic-only.
- `legacy_summary` is compatibility-only and cannot be mistaken for the Core MVP product surface.
- Block 3 product keys are present in `stress_report.json`.
- Diagnostic-mode raw and product stress outputs do not expose active mandate/pass-fail row fields.
- Legacy mandate mode remains gated by `loss_gate_mode="mandate"` tests.
- Scenario Library documentation/output pattern is aligned to `scenario_library_meta` + sidecar JSON.
- No optimizer-first behavior was reintroduced by this cleanup.

## Remaining Notes

- Generated outputs under `Main portfolio/` were regenerated for live verification only.
- Stale generated artifacts elsewhere, if any, remain out of scope and were not edited.
- Global pytest is not clean due to the nine failures listed above; this acceptance audit does not
  classify or fix them because Session 6 scope is Core MVP Blocks 1-3 cleanup verification only.
