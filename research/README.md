# Research

Папка для **разовых** исследований и черновых скриптов вне production-пайплайна.

Старые эксперименты под блочную архитектуру, risk budgeting и `block_selection` **удалены** (2026-04). Для сравнения ковариации / resampling используйте корневые `compare_covariance_shrinkage_main.py`, `compare_robust_cov_main.py`, `compare_resampled_optimization_main.py` вместе с `run_optimization.py`.
