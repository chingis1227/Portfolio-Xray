# M9 — пост-рефакторинг (2026-04-27)

Профиль: **Balanced** (`config.yml`). Универсум: текущие `tickers` в `config.yml` (15 риск-активов + BIL).

**Примечание:** для прохода `run_optimization` удалён устаревший `Main portfolio/portfolio_weights.yml` (тикеры не совпадали с `config.yml`). Baseline «до рефакторинга» в этой таблице **не пересчитывался** — при необходимости сравните с сохранённым прогоном из git / бэкапа.

## Сводка (10Y, `snapshot_10y.json` / `run_result.json`)

| Метрика | Main (post) | EW (post) | RP (post) |
|--------|-------------|-----------|-----------|
| Vol (годовая), 10Y | 0.077 | 0.131 | 0.067 |
| MaxDD (10Y) | -0.126 | -0.187 | -0.11 |
| Sharpe (10Y) | 0.759 | 0.819 | 0.805 |
| Top1 RC (доля дисперсии, снимок) | VOO 10.4% | COPX 15.3% | GLD 7.8% |
| Equity shock PnL (портфель) | -13.94% | -23.58% | -11.49% |
| Stress overall | DIAG_ATTENTION (RC Top1) | DIAG_ATTENTION (loss + RC) | DIAG_ATTENTION (RC Top1) |
| Main `run_result.status` | OK_FALLBACK (RC post / stress diag) | — | — |

## Источники на диске

- Main: `Main portfolio/run_result.json`, `Main portfolio/snapshot_10y.json`, `Main portfolio/stress_report.json`
- EW: `equal-weight portfolio/snapshot_10y.json`
- RP: `risk parity portfolio/snapshot_10y.json`

## Наблюдение для M10 (decision point)

Без блочного RB одностадийный **Balanced** даёт **~25.6% BIL**, заметные веса в **GLD/SCHP/VDC** и пачку «равных по порядку» equity-RC около 10% — профиль ближе к **диверсифицированному carry**, чем к явному «трём блокам». Диагностический стресс часто ловит **RC Top1** на сценариях (порог 10%), при этом **mandate MaxDD** проходит.

Дальнейшая «замена архитектуры» (если нужна) — отдельное решение после просмотра PDF/комментариев.
